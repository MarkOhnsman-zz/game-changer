const { AppState } = require("../AppState")
const { logger } = require("../utils/Logger")

class CharactersService {
  updateCharacter(id, update) {

    if (index === -1) {
      logger.error("invalid id")
      return
    }
  }
}